<%= form_for @comment do |f| %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
<%=f.hidden_field :article_id, value: params[:id] %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
